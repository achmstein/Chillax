# Caddy reverse proxy configuration for Chillax
# Domain is configured via environment variable in .env
#
# Caddy handles TLS termination (auto Let's Encrypt).
# The mobile-bff (YARP) handles all path-based routing internally,
# including /api/* routes and /auth/* (Keycloak).

{$DOMAIN} {
	reverse_proxy mobile-bff:8080
}
